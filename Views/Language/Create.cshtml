@model ProjectManagementSystem.Models.Language

@{
    ViewData["Title"] = "Create Language";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4" style="max-width: 600px;">
    <h3 class="mb-4">➕ Create New Language</h3>

    <form asp-action="Create" method="post" class="shadow p-4 rounded bg-white">
        @Html.AntiForgeryToken()

        <div class="mb-3">
            <label asp-for="LanguageName" class="form-label fw-semibold">Language Name</label>
            <input asp-for="LanguageName" class="form-control" placeholder="Enter language name" />
            <span asp-validation-for="LanguageName" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="ProjectType_pkId" class="form-label fw-semibold">Project Type</label>
            <select asp-for="ProjectType_pkId" class="form-select" asp-items="ViewBag.ProjectTypes">
                <option value="">-- Select Project Type --</option>
            </select>
            <span asp-validation-for="ProjectType_pkId" class="text-danger small"></span>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <button type="submit" class="btn btn-success px-4">Create</button>
            <a asp-action="Index" class="btn btn-secondary px-4">Cancel</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
@model ProjectManagementSystem.Models.Company
@using Microsoft.AspNetCore.Mvc.Rendering

@{
    ViewData["Title"] = "Add Company";
}

<h2 class="mb-4">Add Company</h2>

<form asp-action="Create" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()

    <div class="mb-3">
        <label asp-for="CompanyName" class="form-label"></label>
        <input asp-for="CompanyName" class="form-control" />
        <span asp-validation-for="CompanyName" class="text-danger"></span>
    </div>

    @* <div class="mb-3">
        <label asp-for="Address" class="form-label"></label>
        <input asp-for="Address" class="form-control" />
        <span asp-validation-for="Address" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Contact" class="form-label"></label>
        <input asp-for="Contact" class="form-control" />
        <span asp-validation-for="Contact" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Description" class="form-label"></label>
        <textarea asp-for="Description" class="form-control"></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>

    <!-- ✅ City Dropdown -->
    <div class="mb-3">
        <label asp-for="City_pkId" class="form-label">State/Region</label>
        <select asp-for="City_pkId" class="form-select" asp-items="ViewBag.CityList">
            <option value="">-- Select State/Region --</option>
        </select>
        <span asp-validation-for="City_pkId" class="text-danger"></span>
    </div>

    <!-- Company Image Upload -->
    <div class="mb-3">
        <label class="form-label">Company Photo</label>
        <input type="file" name="imageFile" class="form-control" />
    </div>
 *@
    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

@* @model Announcement

@{
    ViewData["Title"] = "Announcement Details";
    Layout = "_TeacherLayout";
}

<div class="container py-3">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4>Announcement Details</h4>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.AnnouncementId" class="btn btn-primary">
                <i class="fas fa-edit"></i> Edit
            </a>
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </div>
    </div>

    <div class="card">
        <div class="card-header @(Model.BlocksSubmissions ? "bg-danger text-white" : "bg-primary text-white")">
            <h5>@Model.Title</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Message</dt>
                <dd class="col-sm-9">@Model.Message</dd>

                <dt class="col-sm-3">Created Date</dt>
                <dd class="col-sm-9">@Model.CreatedDate.ToString("f")</dd>

                <dt class="col-sm-3">Start Date</dt>
                <dd class="col-sm-9">@Model.StartDate.ToString("f")</dd>

                <dt class="col-sm-3">Expiry Date</dt>
                <dd class="col-sm-9">@(Model.ExpiryDate?.ToString("f") ?? "No expiry date")</dd>

                <dt class="col-sm-3">Status</dt>
                <dd class="col-sm-9">
                    @if (Model.IsActive)
                    {
                        <span class="badge bg-success">Active</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Inactive</span>
                    }
                </dd>

                <dt class="col-sm-3">Blocks Submissions</dt>
                <dd class="col-sm-9">
                    @if (Model.BlocksSubmissions)
                    {
                        <span class="text-danger"><i class="fas fa-check-circle"></i> Yes</span>
                    }
                    else
                    {
                        <span class="text-success"><i class="fas fa-times-circle"></i> No</span>
                    }
                </dd>
            </dl>
        </div>
    </div>
</div> *@